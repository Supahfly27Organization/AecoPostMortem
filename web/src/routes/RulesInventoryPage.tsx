import { ComingSoon } from './ComingSoon'

/** The Rules Inventory (FR-40): every extracted rule statement, its status, its sessions. Built
 * by S-22 — the one surface of the three whose real content lands in Release 2, not Release 1. */
export function RulesInventoryPage() {
  return <ComingSoon surface="Rules Inventory" story="S-22" release="Release 2" />
}
